{
  "name": "solarium",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Solarium — 3D solar system + classical aspect theater (walking skeleton)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "astronomy-engine": "^2.1.19",
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.2.4"
  }
}
